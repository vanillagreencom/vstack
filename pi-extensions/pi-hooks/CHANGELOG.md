# Changelog

## Consumer-impacting changes

### 0.12.0

- **Breaking**: the `hookTimeoutMs` setting is removed. A registered hook's budget is the `timeout` its registration in `kendex/hooks.json` declares, 60 seconds where it declares none, the same budget Claude Code gives it. A value left in `settings.json` is ignored. `runHook` drops its `ceilingMs` argument.

### 0.11.0

- The carrier dispatches the rendered `kendex/hooks.json` registry on every listener kendex maps a hook event onto, not only `tool_call`. A hook declared on `PostToolUse`, `Stop`, `TaskCompleted` or `SessionStart` for Pi was registered and reported enforced and ran nothing; it now runs. Pi refuses nothing on those three events, so a hook's word is delivered rather than obeyed: `tool_result` appends it to the tool result the model reads, `turn_end` steers it into the run with `triggerTurn: true` so a headless session answers for it in the next turn, and `session_start` adds it to the session's opening context without holding the session open. Every registration on those listeners runs — there is nothing to refuse, so nothing stops at the first. Exit `2`'s stderr is the word, exit `0`'s stdout is, and any other status, a run past its budget and a rendered script no scope holds included, is reported rather than read as an all-clear; stderr beside a `0` stays an advisory for the person. `tool_call` is unchanged and still gates. Payloads are Claude Code's for each event: `tool_response` after a tool call, `{source}` at session start, `stop_hook_active` at turn end. Matchers are read against the word Claude Code writes them against — the tool on `tool_call` and `tool_result`, the session source on `session_start` (Pi's `reload` is `resume`, its `new` and `fork` are `clear`) — and `Stop` and `TaskCompleted` take none, so every `turn_end` registration covers the turn. `turn_end` is the registry key, and the carrier reads it on `agent_settled`: Pi's own `turn_end` fires once per LLM turn from inside the tool loop, while `Stop` and `TaskCompleted` mean the agent has finished responding, so a request taking ten tool-calling rounds consults those registrations once rather than eleven times. The steer is spent once per consultation — the dispatch it caused reports without steering again, and says `stop_hook_active: true`, the field a `Stop` hook reads to know it is already the reason the agent kept going — so a response reaches at most two dispatches whatever a hook says.
- The end-of-turn clippy check and the session-start drift report are unchanged and independent of the registry: they port catalog hooks whose own `harnesses:` line leaves Pi out, so nothing registers those two for Pi and nothing runs twice. Install your own copy of either and both run; `taskCompletedCheck` and `sessionDriftCheck` now turn off the registered copy as well as the native one.
- **Breaking**: `runRegisteredHook` is replaced. The spawn and its outcome are `runHook` in the new `extensions/dispatch.ts`, which also exports `runListener`, `agentLine`, `personLine`, `unreadableLine`, `HookOutcome`, `HookResult`, `ListenerRun` and `GUARD_SETTING_NAMES`; `extensions/hooks.ts` exports `toolCallVerdict`, which turns one outcome into a tool-call verdict, and re-exports `GUARD_SETTING_NAMES`. `extensions/registry.ts` adds `TOOL_RESULT_LISTENER`, `TURN_END_LISTENER` and `SESSION_START_LISTENER`, and `registeredHooks`'s second argument is the matcher subject rather than a tool name, `undefined` where the listener has none. `extensions/vocab.ts` adds `claudeSessionSource` and `PI_SESSION_REASONS`.

### 0.10.0

- **Breaking**: `renderedName(root, command, anchor)` takes the project the registry was read from in place of its `scope` argument, `undefined` at global scope. kendex now writes a project hook's command as the script's place under the project root and a walk that finds that file when the hook runs — `$(git rev-parse --show-toplevel)` answered with nothing in a project that is no git repository and with the wrong tree's root in one below the git top level, and an absolute path would make a registry a repository commits one machine's. So a command is one of kendex's when the file it names, resolved against that anchor, is the one the registry's own root holds. A project registry rendered by an older kendex is no longer recognised: its command is run through a shell as written, which in the two layouts above names a file that is not there and refuses every matched tool call, and a per-guard toggle no longer applies to it. `kendex refresh` rewrites the registry; run it before or with this upgrade.

### 0.9.0

- **Breaking**: on a tool call the carrier dispatches the `tool_call` key of the rendered `kendex/hooks.json` registry instead of a fixed list of three script names, so every `PreToolUse` hook kendex installs for Pi fires. A `[[custom-hooks]]` entry has no file of its own — kendex registers your command verbatim, and it exists nowhere but that registry — so it ran nothing at all before while kendex reported it enforced; the same held for any catalog hook whose name was not `block-bare-cd`, `block-repo-copy` or `pre-commit-check`. The other listeners kendex maps hook events onto — `tool_result`, `turn_end`, `session_start` — are not dispatched, so a `PostToolUse`, `Stop`, `TaskCompleted` or `SessionStart` hook still does not run under Pi. The project's registry is read only in a workspace Pi reports trusted, since a hook there is code the project ships; the global registry answers either way. Each registration's `matcher` decides which tool calls it judges, as a whole-string regex over the tool named by kendex's own vocabulary table — Pi's `find` is `Glob`, while its `powershell` has no Claude Code name and is spelled as Pi spells it; absent, empty and `*` cover every tool, and one that will not compile judges the call rather than being skipped. The input keying is this carrier's own and is one rename, the `path` of a `read`, `write` or `edit` reaching the hook as `file_path`; nothing else is reshaped, so an `edit` arrives with Pi's `edits` array rather than Claude Code's `old_string`/`new_string` pair. Exit-status handling is unchanged: `2` refuses with the hook's stderr as the reason, `0` allows, anything else refuses because a guard that did not run does not stand aside — a registry that exists and cannot be read and a registered hook whose rendered script no scope holds included; only an absent registry allows. A refusal names a command-bodied hook by where it is registered, never by its text, which can hold a secret written inline. `hookTimeoutMs` is now the ceiling over a registration's own `timeout` rather than the only budget, so a hook runs to the time it asks for and nothing runs past yours. `runRenderedHook` is replaced by `runRegisteredHook`, which takes a `RegisteredHook` from the new `extensions/registry.ts`.

### 0.8.0

- **Breaking**: the three bash guards are no longer reimplemented in TypeScript. `blockBareCd`, `blockRepoCopy` and `preCommitCheck` now spawn the rendered `<project>/.pi/kendex/hooks/<name>.sh`, falling back to `<PI_CODING_AGENT_DIR or ~/.pi/agent>/kendex/hooks/<name>.sh`, with the payload Claude Code sends a `PreToolUse` hook, and read the exit status: `2` refuses the tool call with the script's stderr as the reason, `0` allows it, and any other status refuses too, since a guard that did not run does not stand aside. A run past the budget refuses whatever status it carries: a killed process still has one, so a hook that traps the signal and exits `0` is a run that judged nothing rather than one that allowed. That budget is the new `hookTimeoutMs` setting, default 60000, and `runRenderedHook` now takes it as a fourth argument. A script writing to stderr and still exiting `0` is an advisory, shown through the UI and never to the agent. A project-scope script runs only in a workspace Pi reports trusted, since spawning it executes code the project ships; the global root answers either way, because it holds the person's own files. The project is resolved the way `kendex` itself resolves it (`crates/core/src/discover.rs`): a `.kendex-lock.json` wherever it stands, otherwise the nearest ancestor holding one of kendex's harness marker directories, and home itself is not a project however else it is marked. A `.git/` is not one of them. So a session started in a subdirectory gets the same guards and the same project `settings.json` as one started at the repository root, and a session in no project gets no project scope at all. The global root is `~/.pi/agent` unless `PI_CODING_AGENT_DIR` names a root-anchored path — a drive or UNC share on Windows, a leading `/` on POSIX; empty, whitespace-only, relative and driveless-rooted values use the default, and `~` expands to your home directory. So no value makes the person's own scope follow the session's directory implicitly; naming a directory outright still points it there. A name neither root holds runs nothing. The npm package ships the carrier, not the scripts, so install the hooks with `kendex add --hook block-bare-cd --hook block-repo-copy --hook pre-commit-check`. Claude Code, Codex and Pi now run the same bytes, so these guards can no longer disagree.
- **Breaking**: `extensions/bash-guards.ts` and `extensions/repo-copy-guard.ts` are removed, with `scanCommand`, `preCommitGate`, `CommandScan`, `PreCommitVerdict`, `isBareCd`, `repoCopyRefusal`, `refusalReason`, `isScratch`, `dangerousMarkers` and `classifySegment`. `extensions/process.ts` `runCommandAsync` takes an optional fifth argument, a string written to the child's stdin.
- **Breaking**: the pre-commit gate reads whitespace-separated words rather than shell, following `hooks/pre-commit-check.sh`. A `git` word with a later `commit` word is the commit, and the bypass is the no-verify flag, a short cluster holding its letter, or a word carrying a `core.hooksPath` key (an attached -c value, the value after a bare -c, a --config-env, a `git config` argument, a `GIT_CONFIG_*` assignment). Nothing else about `-c` is read, so `git commit -c HEAD` and a `user.name` key now pass. One rewrite runs first: every metacharacter bash(1) lists that is not whitespace (`| & ; ( ) < >`) becomes a space, so `true;git commit --no-verify` and `git commit&>/dev/null -n` refuse as the bare flag does; nothing is deleted. A word is seen only where the command already spells it, so a bypass the shell would join, unquote or expand into the word is not seen here and reaches git, which skips its armed hooks. The reading runs the other way: a `git` word, a `commit` word and a bypass word the split leaves standing each count wherever they stand, a commit message, a heredoc body and a comment tail included, and quoting does not change that on its own. So `git log | grep commit`, `git log --oneline "(commit)"` and `git log --grep="<commit>"` all refuse where nothing is armed. Git's armed hooks are the control either way.

### 0.7.0

- **Breaking**: the `preCommitCheck` listener carries the same contract as kendex's `pre-commit-check` bash hook. It reads a commit out of a command's live words, defers to the working directory's git hooks when kendex armed them (`pre-commit` and `commit-msg` both executable and carrying the growth-guards marker, `core.hooksPath` unset), refuses a commit whose live words include one that bypasses an armed hook (`--no-verify`, `-n`, `-c`, `--config-env`, a `GIT_CONFIG_*` assignment, a `core.hooksPath` key), and refuses a commit in a repository nothing armed, naming `kendex guard install`. It no longer runs `cargo fmt --check` or `cargo clippy`, so a Pi commit in an armed repository is validated once, by git; a repository that relied on the listener's own fmt and clippy run arms its hooks with `kendex guard install`. It splits the command into simple commands and judges only their live words, so a heredoc body and a comment tail are text and a quoted word is a live word whose text is its unquoted content. A bypass is a word whose WHOLE content is one, so `git commit "--no-verify"` refuses while `cat -n` in a heredoc, another program's `-c`, and a commit message naming `--no-verify` all pass. It models no argv, so a construct it does not recognise leaves the words standing and is judged; whole words decide, so `-mnote` is a message and `--grep=commit` is not a commit. Constructs it cannot read are the exception, refused unread rather than parsed where the command names git and carries a commit, either in its text with the quote characters removed or in a live word, so a spelling the shell assembles counts and an anchored grep for something else passes; an alias config key carried inline (`-c alias.x=...`) needs only the git, since it renames the subcommand of that invocation, while one written to the config is judged behind the commit word like any other text. Its blind spot is the other way round: text a shell would run but this drops, inside quotes (`sh -c '…'`) or a heredoc body. `cd "$dir"` and `$(…)` pass through to git, which runs the hook in the right repository. The gate judges the working directory only; a commit aimed elsewhere from a directory that is not a repository passes with a TUI notice naming the directory judged. `clippyTimeoutMs` no longer applies to the pre-commit gate.
- **Breaking**: `extensions/bash-guards.ts` exports `scanCommand(command)` returning `CommandScan` (`commit`, `moves`, `bypass`) and `preCommitGate(command, cwd)` returning `PreCommitVerdict` (`allow` with an optional `notice`, or `refuse` with a `reason`); `runPreCommitCheck`, `resolveProjectGitCommit`, `projectGitCommitCwd`, `gitCommitTargets`, `nearestCargoManifestDir`, `GitCommitTarget`, `ProjectGitCommitProbe` and `BlockReason` are removed. `extensions/cargo.ts` drops `runCargoAsync`, `findCargoWorkspaceRootAsync`, `findCargoWorkspaceRootResultAsync`, `runWorkspaceClippyAsync` and `WorkspaceRootResult`.
- The README's settings screenshot (`assets/hooks-settings.png`) is removed; it showed the old "Pre-commit fmt + clippy" option.
- The end-of-turn check steers its summary to the agent with `sendMessage(..., { triggerTurn: true })`, so a headless session (`pi -p`, a subagent) reads clippy's errors in the turn that follows rather than exiting without them; since pi#8022 a `triggerTurn: false` message is recorded without steering, which an ending run never reads. An interactive session still gets the UI notification, and the message carries `display: false` so it is not shown twice. Every failing turn reports, an unchanged failure included: an agent stuck on an error hears the same advisory each turn, which is noisy and self-correcting, where withholding a repeat can leave a headless turn told nothing when there was something to say. A turn that wrote no `.rs` file runs no clippy and reports nothing, so a further report costs an edit. A run that established nothing about the tree — no workspace root, a timeout, or a clippy failure printing no error line — now says so rather than reading as a clean turn.
- **Breaking**: `extensions/lint-hooks.ts` replaces `workspaceClippyErrors` with `workspaceClippyOutcome`, returning `ClippyOutcome` (`clean`, `errors` with `lines`, or `unavailable` with a `reason`) instead of a string array whose empty case meant both a clean tree and a check that never ran.
- `findCargoWorkspaceRoot` no longer caches a failed lookup. A session that starts without cargo on `PATH`, or before a manifest exists, cached the failure for the rest of the session and never found the workspace afterwards.
- **Breaking**: the `postEditLint` listener and its setting are removed; a `.rs` write no longer runs `cargo clippy`. The end-of-turn check still runs it once per turn, and `clippyTimeoutMs` now bounds that run alone. `extensions/lint-hooks.ts` drops `clippyIssuesForFile`, `pathTokensForFilter` and `clippyDiagnosticsForTokens`; `extensions/cargo.ts` drops `invalidateClippyCache` with the per-turn clippy cache the two lanes shared.

### 0.6.1

- `blockBareCd` refuses a `cd` with no target. It changes to `$HOME` for every later tool call, the same permanent move as `cd /tmp`, and only the form carrying a path was caught before.

### 0.6.0

- The session-start drift report no longer opens with "kendex check could not run" when kendex ran and answered. Exit 1 (drift, or packages not yet evaluated) is relayed verbatim. Exit 2 means kendex could not check, in part or at all: a report carrying a "could not check" section is relayed under a "kendex check incomplete (exit 2); some drift status unknown:" line, while output opening with kendex's own `Error:` line or a usage `error:` (nothing was checked) keeps the "kendex check could not run (exit 2)" line. Exit 3+, a non-ENOENT spawn error, an inaccessible session directory, or an unexpected throw read as "could not run"; a missing binary (ENOENT) reads as "skipped". `DriftCheckResult` gains the `incomplete` variant.
- **Breaking**: the `sessionDriftAvailable` setting is removed, along with `driftCheckArgs` and `DriftCheckOptions.includeAvailable`. It passed `--no-available`, a flag `kendex check` has never had, so every session start with it off was a usage error.

### 0.5.0

- **Breaking**: the settings namespace is renamed from `vstack` to `kendex`, with no compatibility fallback. Configuration previously read from `vstack.extensionManager.config["@vanillagreen/pi-hooks"]` in `.pi/settings.json` is now read from `kendex.extensionManager.config["@vanillagreen/pi-hooks"]`; settings still stored under the old key are ignored and this package silently falls back to its defaults until the key is renamed. The `package.json` block that declares these settings is renamed from `"vstack"` to `"kendex"` to match.
- **Breaking**: cross-extension interop symbols move from the `vstack.*` to the `kendex.*` `Symbol.for` registry (`kendex.pi-hooks.installed`, `kendex.pi.project-trust`). Symbol identity is the interop contract, so a package on the old namespace cannot see one on the new namespace — upgrade every installed `@vanillagreen` Pi extension together rather than one at a time.
- Project-root detection recognizes `.kendex-lock.json` instead of `.vstack-lock.json`.
- Repository, homepage, issue-tracker, and README asset URLs now point at `vanillagreencom/kendex`.

### 0.4.0

- New `sessionDriftCheck` setting (default on, category Session): on `session_start` with reason `startup`, `new`, or `fork` (never `resume` or `reload`) runs `kendex check --quiet` in the session cwd without awaiting it (startup is never blocked) and, when it exits 1, appends the drift report to the agent's context as a `kendex-drift` custom message (`triggerTurn: false`); exit 2+ appends a "kendex check could not run" line with the diagnostic; a missing `kendex` binary appends one "kendex drift check skipped: kendex is not on PATH" line; exit 0 is silent. Companion settings `sessionDriftAvailable` (default on; off passes `--no-available`) and `driftCheckTimeoutMs` (default 30000). A session cwd that is missing or cannot be entered appends its own "kendex check could not run: project directory <dir> is not accessible; drift status unknown" line instead of being reported as a missing binary. An unexpected throw anywhere in the drift path appends "kendex check could not run: <reason>; drift status unknown" rather than falling silent, so no failure mode of the check reads as a clean install. New module `extensions/drift-check.ts` exporting `runDriftCheck`, `driftCheckArgs`, `driftMessage`, `driftErrorMessage`, `deliverDrift`, and types `DriftCheckResult` (variants `clean`, `drift`, `failed`, `unavailable`, `unusable-cwd`), `DriftCheckOptions`. New module `extensions/process.ts` exporting `runCommandAsync(command, args, cwd, timeoutMs)` and `CommandResult`; `extensions/cargo.ts`'s `runCargoAsync` delegates to it and `CargoResult` is now an alias of `CommandResult`. On timeout `runCommandAsync` signals the child's process group with SIGTERM and escalates to SIGKILL one second later only while the run has not already settled, so a child that exits on SIGTERM is never escalated and no escalation outlives the run. Mirrors `hooks/session-drift-check.sh` for Pi. The check itself never touches the project's git and never waits on the network; a kendex source cache under `~/.kendex/cache` older than its TTL is refreshed by a detached background process.

### 0.3.0

- New `blockRepoCopy` setting (default on, category Bash): refuses a recursive copy — `cp -r`/`-R`/`-a`, recursive or archive `rsync`, `git clone` of a local path, or a `tar` create-to-extract pipe — when the source carries repository history or a build tree AND the destination resolves under a temp/scratch root (`/tmp`, `/var/tmp`, `$TMPDIR`, `$CLAUDE_CODE_TMPDIR`, a `mktemp -d`, or any path containing `scratchpad`). Temp roots are commonly RAM-backed tmpfs, where such a copy fills the filesystem and every process writing there fails with ENOSPC. Both halves of the predicate are required, so an expensive tree copied elsewhere and an ordinary directory copied into scratch both pass. New exports from `extensions/repo-copy-guard.ts`: `repoCopyRefusal`, `refusalReason`, `isScratch`, `dangerousMarkers`, `classifySegment`. Mirrors `hooks/block-repo-copy.sh` for Pi.

### 0.1.6

- Baseline: changelog introduced at this version. Consumer-impacting changes — behavior deltas, new/renamed/removed exports, settings and config changes, protocol/audit-shape changes — are recorded here from this version forward.
